# MomentBid
MomentBid is a pre-commitment conditional auction protocol for broadcast ad inventory, built on the WireFluid blockchain. It enables brands to commit sealed bids before a PSL match for specific cricket event types (over breaks, wicket falls, last-over thrillers, etc.)
